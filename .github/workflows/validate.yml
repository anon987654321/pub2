name: Validate Code Quality

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Node.js 20
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Setup Ruby 3.2
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: '3.2'

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y shellcheck

    - name: Install JavaScript tools
      run: |
        npm install -g eslint jsonlint

    - name: Install Ruby tools
      run: |
        gem install rubocop

    - name: Validate JSON files
      run: |
        echo "Validating JSON files..."
        find . -name "*.json" -type f | while read -r file; do
          echo "Checking $file"
          jsonlint "$file" || exit 1
        done

    - name: Run RuboCop
      run: |
        echo "Running RuboCop..."
        if [ -f ai3/Gemfile ]; then
          cd ai3 && rubocop . || exit 1
          cd ..
        fi
        find . -name "*.rb" -not -path "./ai3/*" | head -1 > /dev/null && rubocop . || echo "No additional Ruby files found"

    - name: Run ESLint
      run: |
        echo "Running ESLint..."
        find . -name "*.js" -type f | head -1 > /dev/null && eslint . || echo "No JavaScript files found"

    - name: Run ShellCheck (excluding zsh scripts)
      run: |
        echo "Running ShellCheck..."
        find . -name "*.sh" -type f | while read -r file; do
          # Skip zsh scripts
          if head -1 "$file" | grep -q "zsh"; then
            echo "Skipping zsh script: $file"
            continue
          fi
          echo "Checking $file"
          shellcheck "$file" || exit 1
        done

    - name: Run HTML/Markdown audit
      run: |
        echo "Running HTML/Markdown audit..."
        node scripts/audit_html_md.js || exit 1